#include <iostream>
#include <cmath>FOR(i,0,n-1)
#define FOR(i,a,b) for(point b)
{
d()
#define MAXN 100
using namespace 
{\n",res+2*PI*r);
}
{;
#define all(x) x.begin(),x.en
	public:);
	p[N+1] = p[
const double PI = acos(-1)
{
	FOR(i,1,N);
class point
	FOR(i,1,N)
double Distanc
		res+=Distance(p[i],
	return sqrt((b.x-a.x)*(b.x-a.x) + (b.y-a.y)*(b.y-a.y));
}
int main()
	solve();
		double x,y
		void read() { cin >> x >> y; }
}p[MAXN+5];e(point a,
void solve()std;i=a; i<=b; i++)
#define FOR2(i,n) 
		p[i].read(
	return 0;
}1];
	int N,i;
	printf("%.2lf
	double a,r,res=0;
	cin >> N >> r;p[i+1]);
